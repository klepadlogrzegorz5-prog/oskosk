name: Build Android APK

on:
  push:
  #   branches: [ "main", "master" ]
  pull_request:
  #   branches: [ "main", "master" ]
  workflow_dispatch: # Umożliwia ręczne uruchomienie z poziomu zakładki Actions

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Klonowanie repozytorium
        uses: actions/checkout@v4

      - name: Konfiguracja środowiska Java (JDK 17)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle' # Automatyczne cache'owanie zależności Gradle

      - name: Nadanie uprawnień do skryptu Gradle
        run: chmod +x gradlew

      - name: Budowanie APK (Debug)
        run: ./gradlew assembleDebug

      - name: Przesłanie pliku APK jako artefaktu
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
          retention-days: 14
